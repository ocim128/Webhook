{
  "name": "webhook",
  "version": "1.0.0",
  "description": "Local webhook capture service with dynamic, user-defined endpoints.",
  "main": "dist/server.js",
  "scripts": {
    "start": "node dist/server.js",
    "dev": "npx ts-node src/server.ts",
    "dev:watch": "nodemon --exec \"npx ts-node\" src/server.ts",
    "dev:frontend": "vite",
    "build": "tsc && npm run build:frontend",
    "build:frontend": "vite build",
    "preview:frontend": "vite preview",
    "typecheck": "tsc --noEmit",
    "typecheck:frontend": "tsc --project tsconfig.frontend.json --noEmit",
    "typecheck:all": "npm run typecheck && npm run typecheck:frontend"
  },
  "keywords": [
    "webhook",
    "express",
    "local"
  ],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^5.1.0",
    "helmet": "^8.1.0",
    "mongodb": "^7.0.0",
    "nanoid": "^5.1.6",
    "raw-body": "^3.0.2"
  },
  "devDependencies": {
    "@types/cors": "^2.8.19",
    "@types/express": "^5.0.6",
    "@types/node": "^25.1.0",
    "nodemon": "^3.1.11",
    "ts-node": "^10.9.2",
    "typescript": "^5.9.3",
    "vite": "^7.3.1"
  }
}